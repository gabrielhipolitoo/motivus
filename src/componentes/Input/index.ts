import { Button } from "./Button";
import { FormRoot } from "./Form";
import { Input } from "./Input";
import { Label } from "./label";

export const Form = {
  content: FormRoot,
  input: Input,
  label: Label,
  button: Button,
};
